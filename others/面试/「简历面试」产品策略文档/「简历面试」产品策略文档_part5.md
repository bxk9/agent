| **处理逻辑** | 1. **任务管理框架**：每个 Skill 启动时创建任务实例，所有中间产物挂在任务实例下<br>2. **上下文保持**：用户跨 Skill 跳转（如简历改写 → 模拟面试）时，自动把简历版本 / JD / 公司画像注入下一 Skill 的初始上下文<br>3. **空间管理判定**：　<br>- 用户主动说"换个话题"或开始新主题 → 任务挂起，可恢复　<br>- 用户切换 App 超过阈值 → 任务保持但不主动唤起　<br>- 用户明确结束 → 任务归档，沉淀到求职记忆<br>4. **任务恢复**：用户回来时，根据任务 ID 自动加载历史上下文，不需重新粘贴 JD/简历 |
| **输出** | - **任务实例**（含上下文快照 / 步骤进度 / 中间产物）<br>- **跨 Skill 上下文注入参数**（下游 Skill 启动时直接读取） |
| **本期判断** | **本期做**。是阶段一/二/三的共同基础设施，不做则各阶段无法跑通 |
| **依赖** | 平台侧任务管理框架；空间管理判断规则需与小V主端对齐 |

---

#### ~~需求 4：PC 生成简历在手机上的下载互传（新增）~~——依赖pc claw整体打通

| 维度 | 详细说明 |
| --- | --- |
| **现状** | 简历在 PC 端生成/改写后落地为 PDF/DOCX，但用户常在手机上查看或转发给 HR，缺乏跨端流转能力 |
| **输入** | 1. **PC 端定制版简历文件**（PDF/DOCX/本地路径）<br>2. **跨端通道**：vivo 互传 / 系统级跨端文件流转 / 账号同步<br>3. **目标设备标识**：用户已登录的同账号手机 |
| **处理逻辑** | 1. **生成完成后提示**：简历渲染完成后，在 PC 端提示"是否同步到手机？"<br>2. **跨端流转**：复用系统级跨端文件流转能力，**不引入新的外发链路**<br>3. **手机端落地**：手机端在指定目录（如"我的简历"）接收文件，并在小V Skill 中提示"已收到 PC 同步的简历"<br>4. **版本一致性**：跨端文件保留同一版本 ID，更新时双端同步 |
| **输出** | - **手机端简历文件**（与 PC 端版本一致）<br>- **跨端流转状态**（成功 / 失败 / 进行中）<br>- **版本 ID**（用于跨端续接） |
| **本期判断** | **本期做（基础版）**。复用系统级跨端能力，开发成本低、用户感知强，是阶段一闭环必要环节 |
| **依赖** | 系统级跨端文件流转能力（vivo 互传） |

---

#### 需求 5：用户打断 → 上下文保留 → 续接（新增 · 交互规范）

| 维度 | 详细说明 |
| --- | --- |
| **现状** | 模拟面试、简历改写均为长流程（10-30 分钟），用户中途接电话/切换 App/想换话题时，缺乏"暂停-恢复"机制，重新进入会丢失上下文 |
| **输入** | 1. **打断信号**：用户主动说"先停一下" / 用户切换 App / 用户长时间不响应（≥ N 分钟）<br>2. **当前任务状态**：当前题目 / 当前阶段 / 已完成内容 |
| **处理逻辑** | 1. **打断识别**：　<br>- 主动打断（明确说"暂停 / 先停"）→ 立即挂起任务　<br>- 被动打断（切 App / 长时间无响应）→ 自动挂起 + 通知"任务已保留，回来时可继续"<br>2. **上下文快照**：挂起时把当前任务实例 / 已完成步骤 / 中间产物全部存入记忆<br>3. **续接策略**：用户回来时给出明确续接 CTA："继续上次的字节产品一面模拟（已完成 3/10 题）"<br>4. **续接选择**：用户可选 ① 接着练 ② 重头来 ③ 跳过本次 |
| **输出** | - **任务挂起状态**（含进度 / 上下文快照）<br>- **续接总结**（标题 / 进度 / 操作按钮）<br>- **用户决策记录**（接着练 / 重头来 / 跳过） |
| **本期判断** | **本期做**。模拟面试、简历改写均为长流程，必须支持打断恢复，否则用户体验崩溃 |
| **依赖** | 任务管理框架（需求 3 提供基础）；交互设计沉淀 |

---

#### 需求 7：定向搜寻岗位进行推送（补充能力）——根据push、日历推动主动服务可做；屏幕识别推荐位，8月可做，依赖屏幕感知建议

| 维度 | 详细说明 |
| --- | --- |
| **现状** | 用户提出希望主端能根据用户求职意图，主动搜寻并推送匹配岗位 |
| **输入** | 1. **用户求职意图**：来自记忆的目标行业 / 目标公司 / 目标岗位 / 期望薪资<br>2. **岗位数据源**：BOSS / 猎聘等三方招聘网站 |
| **处理逻辑** | 1. **岗位主动检索**：基于用户意图主动爬取/订阅三方招聘网站岗位<br>2. **匹配排序**：按 JD 与用户简历匹配度排序<br>3. **主动推送**：原子通知推送匹配岗位 |
| **输出** | - **推送岗位列表**（公司 / 岗位 / 薪资 / 匹配度）<br>- **跳转参数**（拉起对应招聘 App 详情页） |
| **本期判断** | **待评估**。<br>原因：<br>1. **反爬与合规风险高**：主动爬取招聘网站岗位涉及反爬限制与数据合规问题【火山有信源】<br>2. **偏离主线定位**：当前求职 Skill 定位为"求职陪跑"（写简历 → 准备面试 → 复盘），主动找岗位偏离这一主线<br>3. **三方 App 已成熟**：BOSS 等招聘 App 自身的"岗位推荐"功能已较成熟，重做收益有限 |
| **后续建议** | 待评估。可考虑与三方招聘 App 商务合作（API 接入）替代主动爬取，规避合规风险 |

#### ~~需求 8：管理数据 —— PC 数据展示看板~~（新增 · 二期）

| 维度 | 详细说明 |
| --- | --- |
| **现状** | 用户在多场景沉淀了简历版本、JD/公司画像、模拟面试、真实复盘、错题本等大量数据，目前散落在各 Skill 内部，缺乏统一的"全局视图"，用户无法一眼看到自己的求职进度 |
| **输入** | 1. **简历版本流**：来自能力③，每个目标岗位对应的定制版简历 + 状态（草稿 / 已投 / 已面 / 已 offer / 已拒）<br>2. **面试进度流**：来自能力④⑤，每场模拟/真实面试的轮次、时间、状态（已安排 / 已完成 / 已复盘）<br>3. **错题与掌握度**：来自能力⑥，错题本聚类与艾宾浩斯复习掌握度<br>4. **求职履历池**：来自能力⑥，以"公司+岗位"为主键的全链路记录 |
| **处理逻辑** | 1. **看板聚合**：以 PC 端宽屏布局，按"公司 + 岗位"主键聚合所有相关数据，呈现单条求职生命周期<br>2. **多视图切换**：<br>- **简历视图**：所有定制版简历 + 投递状态 + 关联 JD<br>- **面试视图**：模拟/真实面试时间轴 + 各场轮次状态 + 复盘报告入口<br>- **错题视图**：按考点聚类的错题清单 + 复习节奏 + 掌握度<br>- **总览视图**：在投岗位数 / 已面场次 / 待复盘 / 错题待复习 等核心指标<br>3. **状态联动**：用户在看板上更新状态（如"已收到 offer"）→ 反向写回求职记忆，触发后续提醒（如"是否归档此次求职档案"）<br>4. **跨端一致**：PC 端看板与手机端摘要视图共享同一数据源，PC 编辑后手机端实时同步 |
| **输出** | - **PC 端看板页面**（多视图切换 + 全局检索）<br>- **状态更新事件**（写回求职记忆，驱动主动触达）<br>- **导出包**（看板内容可一键导出为 Markdown / PDF 求职档案，复用能力⑥的 `notebook.export_local`） |
| **本期判断** | **🆕[二期]**。本期重点放在阶段一~四的主链路闭环，看板属于"已沉淀数据的可视化呈现"，依赖前序阶段产出，二期纳入。本期可在能力⑥提供的简易"求职履历池"列表（手机端文本视图）作为兜底过渡 |
| **依赖** | PC 端 UI 框架；与手机端的跨端数据同步通道（与需求 4 复用） |

#### 需求 9：生成文件的管理（新增 · 基础设施）——简历/面经/录音/复盘报告的存储与调用

| 维度 | 详细说明 |
| --- | --- |
| **现状** | 当前各能力会陆续产出多种文件：定制版简历（PDF/DOCX）、个性化题库（导出版）、面经摘要、面试录音、转写文本、复盘报告（Markdown/PDF）、错题本导出包等。这些文件目前**散落在端侧默认目录**，缺乏统一的命名、存储、版本与索引规范，导致：<br>① 用户找不到自己之前生成过的文件；<br>② 跨场景调用时（如二面接续一面、简历倒查）无法快速定位历史产物；<br>~~③ 跨端流转（PC↔手机）时版本错乱~~ |
| **输入** | 1. **文件来源**：<br>- ③ 简历定向生成/改写：定制版简历 PDF/DOCX + Diff 视图<br>- ④ 面试题库生成：个性化题库（结构化 JSON / 导出 Markdown）<br>- ⑤ 真实面试录音复盘：原始录音、ASR 转写文本、复盘报告<br>- ⑥ 求职记忆：错题本导出包、求职档案 Markdown<br>2. **关联元数据**：公司名 / 岗位名 / 轮次 / 生成时间 / 关联 JD ID / 关联简历版本 ID / 关联面试事件 ID<br>3. **用户操作信号**：保存 / 重命名 / 删除 / 收藏 / 分享 / 跨端同步 |
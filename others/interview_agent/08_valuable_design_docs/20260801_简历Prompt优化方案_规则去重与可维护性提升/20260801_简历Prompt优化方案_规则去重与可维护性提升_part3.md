| 7 | 核查提醒话术 | 完全一致 | ~7 行 |
| 8 | 荣誉奖项规则 + 小标题规则 + 禁止重复渲染 | 完全一致 | ~6 行 |
| 9 | 模板选择规则（颜色/风格映射） | **已知不同** | 0 行 |
| 10 | 强动词参考表 + 量化锚点参考表 | **仅 generator 有** | 0 行 |
| **合计** | | | **~189 行** |

**实际缩减率**：189 / 2138 ≈ **8.8%**（远低于原方案估计的 17%）

### 11.2 三个核心目标 → 方案贡献度矩阵

| 目标 | 阶段二（抽取公共规则） | 方案 X（扩展框架层校验） | 方案 Y（精简条件分支） | 方案 Z（工具调用前置检查） |
|------|----------------------|------------------------|----------------------|-------------------------|
| 不出现幻觉 | **零** | **中**（扩展字段校验覆盖面） | 低 | 低 |
| 按要求分流/工具调用 | **零** | 低 | **高**（减少 LLM 判断负担） | **高**（框架层兜底） |
| 避免参数传递错误 | **零** | **高**（新增 4 项自动补全/校验） | 低 | **中** |

**结论**：阶段二对三个核心目标均无直接贡献，其价值纯粹是工程可维护性。方案 X 和 Y 的 ROI 更高。

### 11.3 方案 X：扩展框架层参数校验（P1，工作量 1 天）

在 `agent_executor.py` 的渲染工具调用拦截点新增 4 项校验/补全：

| 校验项 | 逻辑 | 类型 |
|--------|------|------|
| `export_format` 自动补全 | 有文件上传时，根据文件扩展名自动设置（.pdf→"pdf"，.docx/.doc→"word"，其他→"word"） | 自动补全 |
| `auto_compress` 自动补全 | 有文件上传且 Agent 未传时，默认设为 `False`（用户未要求压缩时不应压缩） | 自动补全 |
| `original_resume_text` 告警 | 首次渲染时检测是否传入，未传入则 WARN（AI 标记兜底防线缺失） | 仅告警 |
| `section_order` 完整性校验 | 检测传入的 section_order 是否覆盖 resume_content 中的所有模块 | 仅告警 |

### 11.4 方案 Y：精简 Prompt 条件分支（P2，工作量 0.5 天）

将 `resume_opt_head.md` 中高频违规的条件分支从"多层嵌套 if-else"改为"默认行为 + 例外说明"模式，减少 LLM 判断负担。

重点精简区域：
- 导出格式选择规则（L571-577 与 L688-694 重复）
- 模板选择决策树（L696-714）
- 颜色/风格映射表（L641-654）

### 11.5 方案 Z：工具调用前置检查（P3，工作量 1 天，暂缓）

在框架层增加渲染调用前的自动检查：
- 检测到 `render_sidebar_resume` 调用但本 session 未调用过 `parse_resume_file` 且有文件上传 → 自动补调或阻断
- 检测到 `render_sidebar_resume` 调用但未先调用 `query_my_resumes` → WARN 日志

### 11.6 修订后的优先级排序

| 优先级 | 方案 | 目标 | 工作量 | 状态 |
|--------|------|------|--------|------|
| **P0** | 框架层自动补全（4 项） | 参数传递 | 低 | ✅ 已完成 |
| **P0** | 阶段一（分层标记 + 同步机制 + 基线） | 全部 | 低 | ✅ 已完成 |
| **P1** | 方案 X（扩展框架层校验 4 项） | 参数传递 + 幻觉 | 1 天 | ❌ 待实施 |
| **P2** | 方案 Y（精简条件分支） | 分流/工具调用 | 0.5 天 | ❌ 待实施 |
| **P3** | 方案 Z（工具调用前置检查） | 分流/工具调用 | 1 天 | ❌ 暂缓 |
| **P4** | 阶段二（抽取公共规则） | 可维护性 | 1.5 天 | ❌ 可选（出现漂移事故后启动） |

---

**文档版本**：v2.1  
**最后更新**：2026-08-01  
**作者**：AI Assistant（融合两轮审核意见 + 目标导向再评估）  
**审核状态**：已完成，方案 X/Y 进入实施阶段

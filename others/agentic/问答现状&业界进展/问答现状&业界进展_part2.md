OpenHands 这种长期运行的 Agent 非常依赖 **State（状态）**。手机助手的意图识别不能只看当前这一句话，必须结合**设备上下文**。

- **上下文感知路由：**
  - _用户说：_ “把它发给张三。”
  - _无状态识别：_ 无法识别意图。
  - _OpenHands 式识别：_ 路由检查 Context（刚才搜到了航班信息）。
  - _结果：_ 自动判定为 **Hybrid（混合任务）**。第一步从上下文取数，第二步执行发送。